<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <property name="deploymentEnvironment" value="${DEPLOYMENT_ENVIRONMENT:-unknown}"/>
  <property name="serviceVersion" value="${SERVICE_VERSION:-unknown}"/>

  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
  <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="net.logstash.logback.encoder.LogstashEncoder">
      <includeMdcKeyName>trace_id</includeMdcKeyName>
      <includeMdcKeyName>span_id</includeMdcKeyName>
      <includeMdcKeyName>trace_flags</includeMdcKeyName>
      <customFields>{"service":"agent-gateway","service.name":"agent-gateway","service.version":"${serviceVersion}","deployment.environment":"${deploymentEnvironment}"}</customFields>
    </encoder>
  </appender>
  <root level="INFO">
    <appender-ref ref="JSON"/>
  </root>
</configuration>
